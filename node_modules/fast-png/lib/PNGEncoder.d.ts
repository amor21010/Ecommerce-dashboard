import { IOBuffer } from 'iobuffer';
import { IPNGEncoderOptions, IImageData } from './types';
export default class PNGEncoder extends IOBuffer {
    private _png;
    private _zlibOptions;
    constructor(data: IImageData, options?: IPNGEncoderOptions);
    encode(): Uint8Array;
    private encodeSignature;
    private encodeIHDR;
    private encodeIEND;
    private encodeIDAT;
    private encodeData;
    private _checkData;
    private writeCrc;
}
